# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# This file contains environment variables for database connections.
# Copy this file to .env and update with your actual values.
# NEVER commit .env files with real credentials to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONNECTION
# -----------------------------------------------------------------------------
# PostgreSQL connection string format:
# postgresql://[user]:[password]@[host]:[port]/[database]?[options]

# Local Development (PostgreSQL running locally)
DATABASE_URL=postgresql://postgres:password@localhost:5432/pickleball_db

# -----------------------------------------------------------------------------
# CLOUD PROVIDER EXAMPLES (uncomment and modify as needed)
# -----------------------------------------------------------------------------

# Neon (Serverless PostgreSQL)
# DATABASE_URL=postgresql://[user]:[password]@[endpoint-id].us-east-2.aws.neon.tech/pickleball_db?sslmode=require

# Supabase
# DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# Railway
# DATABASE_URL=postgresql://postgres:[password]@[container].railway.internal:5432/railway

# Render
# DATABASE_URL=postgresql://[user]:[password]@[hostname].oregon-postgres.render.com/[database]

# AWS RDS
# DATABASE_URL=postgresql://[user]:[password]@[instance].[region].rds.amazonaws.com:5432/pickleball_db?sslmode=require

# -----------------------------------------------------------------------------
# CONNECTION POOL SETTINGS
# -----------------------------------------------------------------------------
# These settings optimize database connection pooling

# Maximum number of connections in the pool (default: 10)
DB_POOL_SIZE=10

# Seconds before idle connection is closed (default: 20)
DB_IDLE_TIMEOUT=20

# Seconds to wait for a new connection (default: 10)
DB_CONNECT_TIMEOUT=10

# Maximum lifetime of a connection in seconds (default: 3600 = 1 hour)
DB_MAX_LIFETIME=3600

# -----------------------------------------------------------------------------
# SSL CONFIGURATION (for production/cloud databases)
# -----------------------------------------------------------------------------
# Most cloud providers require SSL. Add ?sslmode=require to DATABASE_URL
# or configure SSL certificates below for custom setups.

# SSL mode: disable, allow, prefer, require, verify-ca, verify-full
# DB_SSL_MODE=require

# Path to SSL certificate (if using verify-ca or verify-full)
# DB_SSL_CERT=/path/to/ca-certificate.crt

# -----------------------------------------------------------------------------
# MIGRATION SETTINGS
# -----------------------------------------------------------------------------
# Used by Drizzle Kit for generating and running migrations

# Set to 'production' to disable verbose logging during migrations
NODE_ENV=development
